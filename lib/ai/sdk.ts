import { openai } from '@ai-sdk/openai'

// Export the OpenAI provider for use with AI SDK
export { openai }

// Simple test to ensure the SDK is properly configured
console.log('AI SDK OpenAI provider initialized successfully') 